{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@open-write/openwrite",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server/route.ts",
    "start": "bun run src/server/route.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ai-sdk/openai": "catalog:",
    "ai": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
